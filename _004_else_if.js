var first_name = "Ricky";
var lat_name = "Bobby";
var age = 51;
var first = true;
var last = false;
var empty = "";


if( age  <= 45 ) {
    //if tha age is less than or equal to 40
} else if ( (age > 40) && (age < 50) ) {
    //if age is greater than 40 
    //&& AND, || OR
} else if (age < 1) {
    //otherwise prevent somethin from happening
} else {
    console.log("hi");
}

//COMPARISON operators
// < <= >= == != ! === ->same object?